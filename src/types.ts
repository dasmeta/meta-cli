export enum PROVIDER {
    AWS = 'Amazon Web Services'
}

export enum PLATFORM {
    LINUX = 'linux',
    MACOS = 'darwin',
    WINDOWS = 'win32'
}

export enum GIT_PROVIDER {
    GITHUB = 'github',
    GITLAB = 'gitlab',
    BITBUCKET = 'bitbucket'
}